enum ToastType {
  success,
  warning,
  error,
  info,
  custom,
}

enum PositionToast {
  top,
  bottom,
}

enum AnimationType {
  fromTop,
  fromLeft,
  fromRight,
}

enum ToastLayout {
  ltr,
  rtl,
}
